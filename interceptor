WITH RECURSIVE NumberSeries(NUM) AS (
  SELECT 1
  FROM DUAL
  UNION ALL
  SELECT NUM + 1
  FROM NumberSeries
  WHERE NUM < 5 -- Specify the desired count
)

SELECT your_sequence_name.NEXTVAL
FROM NumberSeries;
